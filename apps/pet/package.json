{
  "name": "mini-pet",
  "version": "0.1.0",
  "private": true,
  "description": "Mini desktop pet (cat) - Electron + TypeScript",
  "main": "dist/main.js",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.renderer.json",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.renderer.json",
    "start": "electron ."
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "electron": "^44.3.0",
    "typescript": "^7.0.2"
  }
}
